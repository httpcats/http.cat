{
  "name": "httpcats-api",
  "version": "1.0.0",
  "description": "HTTP Cats API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && mkdir -p dist/catala && cp -R images favicon.png apple-touch-icon.png dist/",
    "min": "html-minifier -c htmlmin.conf index.html > dist/index.html && html-minifier -c htmlmin.conf notfound.html > dist/notfound.html && html-minifier -c htmlmin.conf catala/index.html > dist/catala/index.html"
  },
  "author": "rogeriopvl",
  "license": "MIT",
  "devDependencies": {
    "html-minifier": "^0.7.2"
  }
}
